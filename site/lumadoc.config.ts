import { defineConfig } from 'lumadoc';

export default defineConfig({
  title: 'LiteIP Graph',
  description: 'Self-hosted Neo4j with Graph Data Science on Cloudflare',
  base: '/',
  srcDir: './src',
  outDir: './dist',
  theme: {
    // Customize theme as needed
  },
});

